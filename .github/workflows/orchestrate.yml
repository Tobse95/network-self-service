name: Orchestrate subnet provisioning

on:
  push:
    paths:
      - "requests/**"
    branches:
      - main

permissions:
  contents: write

jobs:
  orchestrate:
    name: Generate variable files & open IAC pull requests
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - uses: actions/setup-go@v5
        with:
          go-version: "1.21"
          cache: true
          cache-dependency-path: scripts/go.sum

      - name: Build tools
        working-directory: scripts
        run: |
          go mod download
          go build -o ../bin/validate ./cmd/validate
          go build -o ../bin/orchestrate ./cmd/orchestrate

      - name: Detect new/modified/deleted request files
        id: changed
        run: |
          UPSERTED=$(git diff --name-only --diff-filter=AM HEAD~1..HEAD -- requests/ \
            | grep '\.ya\?ml$' || true)
          DELETED=$(git diff --name-only --diff-filter=D HEAD~1..HEAD -- requests/ \
            | grep '\.ya\?ml$' || true)
          echo "upserted<<EOF" >> $GITHUB_OUTPUT
          echo "$UPSERTED" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          echo "deleted<<EOF" >> $GITHUB_OUTPUT
          echo "$DELETED" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Validate before orchestrating
        run: |
          while IFS= read -r file; do
            [ -z "$file" ] && continue
            echo "Pre-flight: $file"
            ./bin/validate --request "$file"
          done <<< "${{ steps.changed.outputs.upserted }}"

      - name: Orchestrate each request
        env:
          SELF_SERVICE_GITHUB_TOKEN: ${{ secrets.SELF_SERVICE_GITHUB_TOKEN }}
        run: |
          while IFS= read -r file; do
            [ -z "$file" ] && continue
            echo "Orchestrating: $file"
            ./bin/orchestrate --request "$file"
          done <<< "${{ steps.changed.outputs.upserted }}"

      - name: Handle deleted requests (subnet removal)
        env:
          SELF_SERVICE_GITHUB_TOKEN: ${{ secrets.SELF_SERVICE_GITHUB_TOKEN }}
        run: |
          while IFS= read -r file; do
            [ -z "$file" ] && continue
            echo "Recovering deleted request from git history: $file"
            git show HEAD~1:"$file" > /tmp/deleted_request.yaml
            echo "Orchestrating removal for: $file"
            ./bin/orchestrate --request /tmp/deleted_request.yaml --action remove
          done <<< "${{ steps.changed.outputs.deleted }}"

      - name: Commit updated state
        run: |
          git config user.name  "self-service-bot"
          git config user.email "self-service-bot@noreply.github.com"
          git add state/vlan-allocations.yaml
          if git diff --cached --quiet; then
            echo "No state changes"
          else
            git commit -m "chore: update subnet allocations [skip ci]"
            git push
          fi
